[pytest]
# Python PBT harness — LINE OA Commerce (Module B5), spec task 1.1.
# Run from this directory so `harness` and `mocks` import as top-level modules.
testpaths = .
python_files = test_*.py
addopts = -ra
