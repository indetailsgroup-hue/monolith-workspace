# Python PBT harness dependencies — LINE OA Commerce (Module B5), spec task 1.1
hypothesis>=6.100,<7
pytest>=8.0,<9
# DB driver for database-layer property tests (lazy-imported in harness.get_connection).
psycopg[binary]>=3.1,<4
