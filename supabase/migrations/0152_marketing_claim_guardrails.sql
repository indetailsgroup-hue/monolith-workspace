-- Migration: marketing_claim_guardrails — ADR-056: เส้น claim ก่อนยิง content (C20 pattern)
-- ทีมการตลาด/SJ ต้องอ่านก่อนโพสต์แรก — roadmap ADR-055 เดือน 2 ยิง TikTok/Facebook 40 คลิป

update public.customer_docs
set body = body || chr(10) || chr(10) ||
  '【claim guardrails การตลาด — เช็คก่อนโพสต์ทุกครั้ง (ADR-056)】' || chr(10) ||
  '✅ พูดได้ (มีหลักฐานในระบบ):' || chr(10) ||
  '  • ราคานิ่ง + วันส่งมอบชัดตั้งแต่วันแรก (ระบบ stamp ลง audit — ยืนยันได้จริง)' || chr(10) ||
  '  • วัสดุมาตรฐาน E0/E1 ปล่อยสารต่ำ — ตามสเปกที่สั่งจริงเท่านั้น' || chr(10) ||
  '  • รูป before/after งานจริง — เฉพาะที่ลูกค้ายินยอมแล้ว (PDPA: consent ก่อนเสมอ)' || chr(10) ||
  '  • ระบบงานมีหลักฐานทุกขั้น สัญญา/ตรวจรับ/ประกัน ตรวจสอบได้' || chr(10) ||
  '⚠️ ระวัง (ขอหัวหน้า review ก่อน):' || chr(10) ||
  '  • ตัวเลขภายใน (margin/ต้นทุน/สถิติลูกค้า) — ห้ามหลุดลง content' || chr(10) ||
  '  • การเปรียบเทียบคู่แข่งแบบระบุชื่อ/แบรนด์' || chr(10) ||
  '❌ ห้ามเด็ดขาด:' || chr(10) ||
  '  • "ปลอดสาร 100%" (ไม่จริง — พูดได้แค่มาตรฐานปล่อยสารต่ำ)' || chr(10) ||
  '  • "ถูกที่สุด/เร็วที่สุดในตลาด" (hard claim พิสูจน์ไม่ได้)' || chr(10) ||
  '  • โชว์แอป/ระบบเป็นจุดขายลูกค้า ถ้าลูกค้ายังไม่ได้ใช้งานส่วนนั้นจริง' || chr(10) ||
  '  • ใช้รูป/asset/ผลงานของคู่แข่ง — ทุกกรณี'
where slug = 'sale_scripts'
  and body not like '%【claim guardrails การตลาด —%';
