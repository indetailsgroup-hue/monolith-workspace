/**
 * Object URL Texture Hook (Stub)
 *
 * Creates a Three.js texture from a Blob/Object URL.
 */

import type { Texture } from 'three';

/**
 * Load a texture from an object URL.
 * Stub: returns null.
 */
export function useObjectUrlTexture(_url: string | null): Texture | null {
  return null;
}
