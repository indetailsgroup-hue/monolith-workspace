/**
 * Cabinet Bounds from Drill Map (Stub)
 *
 * Computes bounding box from drill map data.
 */

import type { DrillMap, DrillMapBounds } from './types';

/**
 * Compute bounding box from a drill map.
 * Stub: returns zero bounds.
 */
export function computeBoundsFromDrillMap(_drillMap: DrillMap, _padding?: number): DrillMapBounds {
  return { width: 0, height: 0, depth: 0 };
}
